<Window x:Class="ControlTaxiDesktop.Navieras.Views.NavierasDepartureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registrar salida"
        Height="270"
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Registrar salida real" FontSize="18" FontWeight="Bold" Foreground="#12324A"/>
        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Hora salida real" FontWeight="Bold"/>
                <TextBox x:Name="DepartureTextBox" Height="34" Margin="0,6,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="PAX salida" FontWeight="Bold"/>
                <TextBox x:Name="PaxTextBox" Height="34" Margin="0,6,0,0"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Observaciones" FontWeight="Bold"/>
            <TextBox x:Name="NotesTextBox" Height="34" Margin="0,6,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Width="100" Margin="0,0,10,0" Content="Cancelar" Click="CancelButton_Click"/>
            <Button Width="120" Background="#0E5E9C" Foreground="White" Content="Registrar" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
